# Second-anchor report ingest, for the first on-air test of the sealed link.
#
# Turns on the WV3 receive path: a sealed, replay-checked distance from the
# satellite reaches ultrawidelock_satellite_report() and is paired with this
# node's own measurement of the SAME ranging block.
#
# Needs the latch overlay alongside it, and that is a current limitation rather
# than a design choice: witness_link.c still holds the WV2 consumer inline, so
# the sealed link does not link without it (see ULTRAWIDELOCK_ANCHOR_LINK's help).
#
# Enrolling the key is a two-step dance because the Thread image has no shell:
#   1. flash the READER image, `ultrawidelock anckey <hex32>` over USB CDC
#   2. flash this image WITHOUT --erase, so the settings partition survives
# Give the satellite the same 32 hex characters with `sat key`.
#
# Build: make build LATCH=1 CDK_CONF="overlay-thread.conf;overlay-latch.conf;\
#   overlays/bench-2resp.conf;overlays/bench-anchorlink.conf;overlay-lto.conf"
CONFIG_ULTRAWIDELOCK_ANCHOR_LINK=y

# bench-2resp.conf turns the console SAT-HANDOFF line on for `sat join` by
# hand; on this overlay the sealed link carries the same handoff itself
# (cherry_ccc_shim.c calls it redundant once that path exists), so the print
# only costs flash and discloses the URSK.
CONFIG_ULTRAWIDELOCK_SATELLITE_HANDOFF_LOG=n

# The console is RTT and nothing on this image opens a UART, so the serial
# driver stack is dead weight the linker cannot prove dead. Freed the room
# the channel classifier (overlays/mlgate.conf) needs to fit the MCUboot
# slot. If a UART consumer ever joins this stack, drop this line first.
CONFIG_SERIAL=n

# Tape-measured on the current bench, antenna centre to antenna centre.
# Without this the Kconfig default (1200) silently mis-sizes every triangle
# check. Re-measure whenever a board moves.
CONFIG_ULTRAWIDELOCK_ANCHOR_BASELINE_MM=1400

# This board read 840 mm at a tape-measured 1000 (phone held at the second
# anchor's antenna, 31 samples). The satellite measured ~0 bias the same way,
# so only this side is corrected. Recalibrate if the DW3110 or its antenna
# changes.
CONFIG_ULTRAWIDELOCK_UWB_RANGE_BIAS_MM=160

# BENCH ONLY, and it weakens what a walk-up proves. The shipping 3000 exists so
# a clear always begins where the two anchors are nowhere near equidistant. On
# this 1400 mm baseline that range is the collinear degenerate zone instead:
# |d_inside - d_outside| approaches the baseline, and the triangle gate rejects
# the pair, so the latch can never see a first agreeing window and refuses for
# ever on ULTRAWIDELOCK_LATCH_R_WINDOWS. 1200 sits inside the well-conditioned
# region for this separation. Raise the anchors apart and put this back before
# reading any result here as evidence about a real door.
CONFIG_ULTRAWIDELOCK_LATCH_CLEAR_MIN_MM=1200

# BENCH ONLY. The shipping 60 s says "an unlock means somebody entered; do not
# passively unlock again for a minute". Iterating on a bench means crossing the
# plane and retrying within seconds, and every retry inside the dwell is burned
# before the evidence is even read. 5 s keeps the property observable without
# making each iteration a coffee break. Put 60000 back on a mounted door.
CONFIG_ULTRAWIDELOCK_LATCH_ENTRY_DWELL_MS=5000

# Sized from measured jitter, which is what the Kconfig help asks for and the
# 90 default never was. Stage C put per-anchor sigma at 61-68 mm, so the
# DIFFERENCE of two anchors carries sigma ~= 92 mm and 250 is a shade under
# 3 sigma. This widens what counts as a possible pair; it does NOT rescue a
# pair that breaks the triangle inequality outright, which is what body
# blocking produces and what the gate must keep refusing.
CONFIG_ULTRAWIDELOCK_ANCHOR_TOL_MM=250
