# Isolate a peripheral-initiated 2M PHY request from DLE and Host ACL geometry.
# The PHY callback records the peer's actual TX/RX selection.
CONFIG_ULTRAWIDELOCK_BENCH=y
CONFIG_ULTRAWIDELOCK_LAT_TRACE=y
CONFIG_BT_CTLR_DATA_LENGTH_MAX=27
CONFIG_BT_AUTO_PHY_PERIPHERAL_2M=y
