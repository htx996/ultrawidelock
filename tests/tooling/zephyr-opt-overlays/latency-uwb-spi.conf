# Bench lane only. The phase trace is an allowlisted compact log record. The UWB
# histograms and SPI counters stay silent on the hot path and are read post-run.
CONFIG_ULTRAWIDELOCK_BENCH=y
CONFIG_ULTRAWIDELOCK_LAT_TRACE=y
CONFIG_DW3000_SPI_METRICS=y
