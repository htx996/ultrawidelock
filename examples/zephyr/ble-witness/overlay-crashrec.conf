# DIAGNOSTIC. Thread names on, so the crash record names the thread that
# died instead of making the reader guess it from the stack pointer. The
# record itself costs nothing to carry: it is written by the fault handler,
# which only runs in a boot that has already failed.
CONFIG_THREAD_NAME=y
