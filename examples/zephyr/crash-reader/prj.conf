# The half of the instrument that can talk. No radio, no MPSL, no crypto:
# this boots the way stock dongle firmware boots, which is the whole point.
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_GPIO=y
